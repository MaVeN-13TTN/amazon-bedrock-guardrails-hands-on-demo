fastapi==0.115.6
mangum==0.19.0
pydantic==2.10.4
pydantic-settings==2.7.0
# 1.38.0 is the minimum that carries both fields this project reads:
#   - `outputScope` on ApplyGuardrail (request) — from 1.37.0; 1.36.26 and earlier
#     reject it with "Unknown parameter in input" (bisected, validation log V-14)
#   - `tier.tierName` on GetGuardrail (response) — from 1.38.0; 1.37.x parses the
#     response but silently drops the field, so the tier reads as absent even
#     though AWS sent it (bisected, validation log V-24)
# A dropped response field is the quieter of the two failures: nothing raises.
boto3==1.38.0
