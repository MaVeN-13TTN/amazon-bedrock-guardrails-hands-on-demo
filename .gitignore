# python
__pycache__/
*.pyc
.venv/
venv/
backend/build/
backend/dist/
backend/.env

# node
node_modules/
.next/
out/
next-env.d.ts
frontend/.env.local
frontend/deploy.zip

# terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl
crash.log

# misc
.DS_Store
.ruff_cache/
*.tsbuildinfo
.pytest_cache/
.hypothesis/

# Kiro spec workspace — requirements, design and task tracking for the build.
# Working notes, not part of what the repository teaches.
.kiro/

# playwright
frontend/test-results/
frontend/playwright-report/

# Slide deck working copy. `deck-stage.js` and `support.js` are generated canvas
# runtime files, not source, and the deck still carries [SPEAKER TO FILL IN]
# placeholders. The 3-page handout in assets/ covers the same ground and is
# finished. Export a PDF here if the deck itself should ship.
assets/kilimo-builder-slide-deck/
